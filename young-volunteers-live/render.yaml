services:
  - type: web
    name: young-volunteers-live
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    numInstances: 1
    envVars:
      - key: NODE_VERSION
        value: 22
