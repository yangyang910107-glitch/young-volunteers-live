PDF 记录准确性修复：全班概览将未提交 Key Ideas 草稿标为 unfinished draft saved；整课任务未提交时保留 lessonDrafts 的原选择或写作，并明确标记未提交。草稿不计入准确率或成绩，公布权限规则不变。

整课写作草稿保存修复：输入后 250ms 自动排队保存，不再在文本框失焦时同步重绘页面；提交会清理延迟保存、等待旧保存队列，再保存最新内容并提交，避免第一次点击提交被失焦重绘打断。保留 Volunteers 60–80 words 提示、原写作任务与答案公布规则。public/lesson-ui.js 语法检查通过；4 项 Volunteers 集成测试全部通过，0 失败。

# 核验记录

refine7：4 项集成测试全部通过，覆盖正常整课、零参与预览/公布、部分草稿超时、互查补做与答案/记录权限。真实浏览器零提交一路走到结尾，D/C 两次 Demo 点击分别呈现一条解释；Demo 不显示六组进度。Key Idea 学生六张卡、无重复答案列表，学生选择 rgb(23,23,23)、参考 rgb(173,53,40)；老师六题共 36 个组选择、6 个参考答案。教师与学生任务标题均用一致的 1–10 编号，示范保持 Example 0。390px 实际视口下三种布局无横向溢出，纸边缩为 6px；临时视口已恢复。浏览器警告与错误为空。原 PPT 导入照片、结尾拼贴与作业策略说明均保留。

个人、全班、未公布/未完成三种 PDF 实际生成并检查 A4 页面边距，未公布字段无 undefined；渲染未完成记录第 2、3 页确认草稿标签、原始证据、私人笔记及隐藏参考答案正常。此次包含 server.js、bridge-reference.js 和 public 修改，因此需要根目录 + public 两包。

frame6：四条边各有四段装饰纸边。浏览器核验十二个老师阶段，全部有 16 段、各环节 sprite 不同、无横向溢出；学生阅读页和老师词汇页在 390 / 760 / 951 / 1280px 下，侧边与任务区域分别至少留 12 / 9 / 20 / 33px，第一题装饰伪元素为 none，浏览器警告与错误为空。实际查看词汇顶端、页脚和学生阅读截图，边框与按钮、阅读证据工具保持分离。手机侧边 6px、桌面 18px，临时浏览器尺寸已恢复。后端未改，此更新只需 public。

frame5：2 项整课集成测试通过，0 失败。浏览器加载十二个老师状态、十二个学生状态，确认各环节使用不同 sprite 位置、桌面页脚面积一致、没有页面报错与横向溢出。单词页第一题没有装饰伪元素；装饰层 aria-hidden / inert / pointer-events:none。实操检查老师开房、学生加入选择提交、实时 1/6、公布同步和下一步换图。检查阅读与 Key Idea 页面文字、划线工具、按钮不被贴纸遮挡。390px 手机下页脚缩为 46px、四角缩为 13px，侧面贴纸隐藏，步骤导航可横向滚动且标签不互相重叠。资源保留透明 alpha。

本轮只改 public 视觉代码与新增素材，根目录代码未改，线上尚未上传。

2 项 Node 集成测试通过，0 项失败：核对 Word 9 题及四篇原文，覆盖整套十二阶段课堂流程。

核验包含：公布前无答案/成绩/范例、六组提交进度、只读旁观者、同组不同题草稿合并、六题划线与 Key Idea、逐个老师示范、互查修改、结尾写作、h/i 出门条对错、私人/全班记录权限、作业阶段和重开活动。

浏览器核验：老师导入 → 学生选择并提交 → 老师实时 1/6 → 老师公布 → 学生结果出现；出门条六组柱状图、并列名次与彩色证据讲解。

真实 PDF 导出器生成整课记录，使用 Poppler 逐页渲染检查。学生记录包含新增整课任务和出门条，全班记录包含六组结果和榜单。所有材料由提供的 PPT 与 Word 提取。

尚未上传 GitHub 或更新线上服务。

compact4 核验：2 项集成测试通过，新增六组完成顺序 1–6、重复提交不改变顺序、重开清除顺序断言。真实浏览器检查教师/学生两轮词汇操作、答案公布同步、三列两行布局、两行步骤导航、教师顶端操作与六组状态、六组总分及逐题对错合并图。1280×720 投屏下两轮教师题目、六组状态均完整可见；学生填空六题与提交按钮完整可见。公布后没有重复题目、旧选项、进度名单和教学说明挡在答案之前。浏览器错误/警告日志为空。

新增拼贴 atlas 使用内置图像生成，透明 PNG 直接复制进 public，没有改动 PPT 原封面。此次包含后端记录完成顺序，因此需要同时更新根目录和 public。

PPT 拼贴视觉更新：使用提供的原封面、志愿者照片和两张贴纸素材；老师与学生入口、标题装饰和导入页面已在浏览器检查。确认登录、选择、提交、老师实时进度仍正常。仅更新前端显示，不改变计分或公布答案规则。
真实浏览器再次核验：填写志愿者角色短文后首次点击提交成功，保留等待教师公布状态，无浏览器警告。
